CREATE TABLE users(
id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
nick VARCHAR(30) NOT NULL UNIQUE,
email VARCHAR(30) NOT NULL UNIQUE,
password VARCHAR(50),
loged TINYINT(1) NOT NULL,
token_black VARCHAR(256),
reg_date TIMESTAMP
);